# dj-set-prep-agent
An AI DJ set-prep assistant: Claude orders a crate of tracks into a set (warmup to peak, harmonic Camelot transitions) by calling the DJ Track Audio Analyzer Actor as a tool through the Apify MCP server.
